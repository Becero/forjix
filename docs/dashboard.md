# Dashboard

Dashboard metrics are calculated from tenant operational tables. No derived metric is persisted. The endpoint `/api/dashboard` requires `dashboard.view` and returns today's sales, average ticket, active products, inventory cost, low stock, open cash, seven-day sales, recent sales and top products.
