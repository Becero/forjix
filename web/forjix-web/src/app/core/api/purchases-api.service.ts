import{HttpClient,HttpParams}from'@angular/common/http';import{inject,Injectable}from'@angular/core';
export interface Supplier{id:string;name:string;document?:string;email?:string;phone?:string;contactName?:string;notes?:string;isActive:boolean}export interface PurchaseItem{id:string;productId:string;productName:string;sku:string;quantity:number;unitCost:number;total:number}export interface Purchase{id:string;number:string;supplierId:string;supplierName:string;status:'Pending'|'Received'|'Cancelled';total:number;notes?:string;userName:string;createdAt:string;receivedAt?:string;cancelledAt?:string;rowVersion:string;items:PurchaseItem[]}export interface PurchaseList{id:string;number:string;supplierName:string;status:string;total:number;itemCount:number;createdAt:string;rowVersion:string}
@Injectable({providedIn:'root'})export class PurchasesApiService{private readonly http=inject(HttpClient);private params(f:Record<string,string|number>){let p=new HttpParams();Object.entries(f).filter(([,v])=>v!=='').forEach(([k,v])=>p=p.set(k,v));return p}suppliers(f:Record<string,string|number>){return this.http.get<{items:Supplier[],total:number}>('/api/suppliers',{params:this.params(f)})}saveSupplier(body:Omit<Supplier,'id'>,id?:string){return id?this.http.put<Supplier>(`/api/suppliers/${id}`,body):this.http.post<Supplier>('/api/suppliers',body)}purchases(f:Record<string,string|number>){return this.http.get<{items:PurchaseList[],total:number}>('/api/purchases',{params:this.params(f)})}create(body:{supplierId:string;notes?:string;items:{productId:string;quantity:number;unitCost:number}[]}){return this.http.post<Purchase>('/api/purchases',body)}action(id:string,action:'receive'|'cancel',rowVersion:string){return this.http.post<Purchase>(`/api/purchases/${id}/${action}`,{rowVersion})}}
